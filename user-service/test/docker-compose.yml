version: "3"

services:
  user-db:
    image: postgres:13
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: peerprep
      POSTGRES_PASSWORD: ${{ secrets.PG_PASSWORD }}